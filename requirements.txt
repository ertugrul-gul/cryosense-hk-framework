# CryoSENSE-HK pinned dependencies (matching manuscript Table S1).
# Python 3.12.10 was used for the reported results.

# Core scientific / modelling stack
numpy==2.4.6
pandas==2.3.3
scipy==1.16.3
scikit-learn==1.8.0
xgboost==3.1.2
shap==0.51.0

# Plotting
matplotlib==3.10.8
seaborn==0.13.2

# Geospatial and climate I/O (study-area map, CMIP6/ERA5 NetCDF)
geopandas==1.1.3
rasterio==1.5.0
xarray==2025.12.0

# Runtime extras required to execute the notebook end to end.
# (Pin to your local `pip freeze` output for a byte-exact environment.)
netCDF4>=1.7
ipython>=8.0
jupyterlab>=4.0
